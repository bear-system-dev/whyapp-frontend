export interface GroupMessage {
  // Nota: É propício que os nomes das propriedades de retorno sigam uma única linguagem
  id?: string
  mensagem?: string
  grupoId?: string
  usuarioId?: string
  createdAt?: string
}
